{{- if eq .Values.global.environment "staging" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-myapp-staging
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  myapp-overview.json: |
{{ .Files.Get "grafana/dashboards/staging/myapp/myapp-overview.json" | indent 4 }}
  myapp-slo.json: |
{{ .Files.Get "grafana/dashboards/staging/myapp/myapp-slo.json" | indent 4 }}
{{- end }}