# This pattern keeps credentials in Secrets and 
# configuration in versioned YAML, which is standard 
# for GitOps-based monitoring setups.
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-smtp-secret
  namespace: monitoring
type: Opaque
stringData:
  smtp_password: "CHANGE_ME_SMTP_PASSWORD"